const left=document.querySelector('#leftPage'),right=document.querySelector('#rightPage'),book=document.querySelector('#book'),counter=document.querySelector('#counter');let sheet=0,sound=false;const total=BOOK_DATA.length;
function esc(s){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function cover(){return `<article class="page cover"><p class="season">TÉCNICA 9 · 1RO 2DA</p><h1>EL AULA QUE<br>APRENDIÓ A ESCUCHARSE</h1><p>Una historia de nueve temporadas</p><p><b>Escritor: primero segunda</b></p><p class="chalk">“Nadie aprende solo porque…”</p></article>`}
function back(){return `<article class="page back"><p class="season">CONTRATAPA</p><h2>La última página también enseña</h2><p>“…nadie llega al final sin que alguien le enseñe una parte del camino.”</p><p>Fin.</p></article>`}
function chapter(ch){return `<article class="page"><p class="season">${esc(ch.season)}</p><h2>Capítulo ${ch.num}<br>${esc(ch.title)}</h2><p><b>Docente narrador/a:</b> ${esc(ch.teacher)}</p><p>${esc(ch.text)}</p><span class="number">${sheet}</span></article>`}
function render(){right.innerHTML=sheet===0?cover():sheet>total?back():chapter(BOOK_DATA[sheet-1]);counter.textContent=sheet===0?'Portada':sheet>total?'Contratapa':`Capítulo ${BOOK_DATA[sheet-1].num} · ${BOOK_DATA[sheet-1].season.replace('Temporada ','T')}`;document.querySelector('#previous').classList.toggle('disabled',sheet===0);document.querySelector('#next').classList.toggle('disabled',sheet>total)}
function go(delta){if(delta>0&&sheet>total)return;if(delta<0&&sheet===0)return;book.classList.remove('flipping');void book.offsetWidth;book.classList.add('flipping');setTimeout(()=>{sheet+=delta;render()},390);if(sound&&'speechSynthesis'in window){speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(delta>0?'Pasando página':'Volviendo una página');u.lang='es-AR';speechSynthesis.speak(u)}}
document.querySelector('#next').onclick=()=>go(1);document.querySelector('#previous').onclick=()=>go(-1);document.addEventListener('keydown',e=>{if(e.key==='ArrowRight'||e.key===' ')go(1);if(e.key==='ArrowLeft')go(-1)});right.onclick=e=>{if(e.clientX>right.getBoundingClientRect().left+right.offsetWidth*.65)go(1)};document.querySelector('#sound').onclick=()=>{sound=!sound;document.querySelector('#sound').textContent=sound?'🔊 Sonido: activado':'🔈 Sonido'};document.querySelector('#fullscreen').onclick=()=>document.documentElement.requestFullscreen?.();render();
